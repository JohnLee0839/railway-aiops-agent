# 《铁路智能运维系统源码详解》

> **基于真实源码的完整导读 | 零基础可读 | 递归解释每一个术语 | Metric-driven AIOps 版**
>
> 项目：SuperBizAgent v2.0.0
> 架构版本：Metric-driven Autonomous AIOps（架构重构版）

---

# 第一部分 项目总体介绍

## 1.1 这个项目是什么？

**项目名称：** `SuperBizAgent`（v2.0.0）

这是一个**铁路信号系统智能运维（AIOps）平台**。它就像一个"永不睡觉的运维工程师"——当铁路信号系统出现异常（如丢包率飙升、通信延迟、信号设备状态异常），它能自动检测问题、**诊断攻击/故障类型**、查找历史案例、制定处置计划、执行修复动作，最后验证结果。

### 当前架构：Metric-driven Autonomous AIOps

系统已完成 **Metric-driven 架构重构**，核心变化：

**旧架构（Attack-driven AIOps）：**
```
STSRS 数据(含 attack_code) → Normalizer 映射 attack_type → 按类型分发 Pipeline
```
- 假设输入时已知攻击类型
- Severity = AttackType 查表（DoS=P1, Replay=P2...）
- TriageAgent 只负责"验证已知类型"

**新架构（Metric-driven Autonomous AIOps）：**
```
STSRS 原始监测指标 → STSRS Adapter(隐藏攻击标签) → Prometheus Simulator
    → EventNormalizer(attack_type=UNKNOWN) → SeverityEngine(指标影响评估)
    → TriageAgent(★ 真正诊断 ★) → attack_type 作为诊断结论输出
```
- 系统只能接收原始监测指标（丢包率、延迟、信号状态等）
- 不知道攻击类型 — 由 TriageAgent 基于异常模式诊断
- Severity 基于实际指标影响程度评估
- TriageAgent 是真正的诊断 Agent，不是"对号入座"的验证工具

**生活化类比：**

旧架构像一个护士直接告诉你"这个病人是心脏病"（提前知道），然后医生按心脏病流程处理。

新架构像一个真正的急诊医生——只能看到生理指标（心率、血压、血氧），必须自己判断是什么病。

### 四层协同架构

1. **Data Ingestion Layer** — STSRS 数据适配 + Prometheus 指标模拟（新增）
2. **Event Driven Workflow Layer** — 确定性故障处理（标准化流程）
3. **Multi-Agent Diagnosis Layer** — 智能诊断（TriageAgent 真正诊断 + RAG + LLM）
4. **Adaptive Recovery + Safety Control Layer** — 异常自主恢复 + 安全兜底

---

## 1.2 核心设计原则：为什么改？

### 问题 1：现实 AIOps 不知道攻击类型
- 真实生产中，Prometheus 只告诉你 `rail_packet_loss=0.35`，不会告诉你"这是 DoS 攻击"
- 旧的 `STSRS_ATTACK_MAP` 假设系统提前知道攻击码 → 攻击类型，这是"看答案做题"

### 问题 2：Severity 不应由 AttackType 决定
- 旧版：`DoS = P1, Replay = P2`（查表）
- 新版：基于实际指标影响 — 丢包率 50% 影响通信(P2)，信号 RED 影响安全(P1)

### 问题 3：TriageAgent 应该是诊断者，不是验证者
- 旧版：TriageAgent 用已知 attack_type 去查 KB → "对号入座"
- 新版：TriageAgent 分析指标异常模式 → 输出 attack_type 诊断结论

### 问题 4：去重不应依赖攻击类型
- 旧版：`dedup_key = attack_type + train_id`
- 新版：`dedup_key = train_id + signal_id + 时间窗口 + 异常指标组合`

---

## 1.3 技术栈全貌

| 层次 | 技术 | 作用 |
|------|------|------|
| Web 框架 | FastAPI | 提供 HTTP API 接口 |
| AI 框架 | LangChain + LangGraph | 编排 Agent 和 Workflow |
| 大语言模型 | 阿里云 DashScope (通义千问 Qwen-Max) | 理解问题、诊断、生成答案 |
| 向量数据库 | Milvus | 存储和检索知识文档 |
| 工具协议 | MCP (Model Context Protocol) | Agent 调用外部工具 |
| 异步运行时 | asyncio + uvicorn | 高并发处理请求 |
| 前端 | 纯 HTML/CSS/JS | 用户交互界面 |
| 日志 | Loguru | 结构化日志输出 |

---

# 第二部分 总体架构

## 2.1 完整数据流（Metric-driven）

```
                        ┌──────────────────────────────┐
                        │    STSRS 数据集 (.csv/.json) │
                        │    (含 AttackInfo 但隐藏)      │
                        └──────────────┬───────────────┘
                                       │
                        ┌──────────────▼───────────────┐
                        │ ① STSRS Data Adapter [NEW]   │
                        │  - 读取多文件                 │
                        │  - 删除 AttackInfo 字段       │
                        │  - Timestamp+TrainID+SignalID │
                        │    三维融合                   │
                        │  输出: RailMetricRecord       │
                        └──────────────┬───────────────┘
                                       │
                        ┌──────────────▼───────────────┐
                        │ ② Prometheus Simulator [NEW] │
                        │  - MetricExporter            │
                        │  - 生成 Prometheus 格式指标   │
                        │  输出: PrometheusMetricSnapshot│
                        │  rail_packet_loss{train="T"} │
                        └──────────────┬───────────────┘
                                       │
        ┌──────────────────────────────▼──────────────────────────────┐
        │              ③ Event Driven Pipeline (核心)                  │
        │                                                             │
        │  ┌──────────────────┐    ┌──────────────────┐              │
        │  │ EventNormalizer  │    │ Deduplicator     │              │
        │  │ attack_type=     │───▶│ 基于 TrainID +   │              │
        │  │ UNKNOWN          │    │ SignalID + 异常   │              │
        │  │ severity=P4      │    │ 指标组合去重      │              │
        │  └──────────────────┘    └────────┬─────────┘              │
        │                                   │                        │
        │  ┌────────────────────────────────▼──────────────────┐    │
        │  │  SeverityEngine (基于 Metric Impact)               │    │
        │  │  P1: SignalStatus=RED → 影响安全                  │    │
        │  │  P2: PacketLoss>50% → 影响通信                    │    │
        │  │  P3: Latency>100ms → 性能下降                     │    │
        │  │  P4: 轻微异常/正常                                │    │
        │  └────────────────────────────────┬──────────────────┘    │
        │                                   │                        │
        │  ┌────────────────────────────────▼──────────────────┐    │
        │  │  TriageAgent ★ 真正诊断 ★                         │    │
        │  │  输入: metrics_snapshot (PacketLoss, Latency...)   │    │
        │  │  分析: 异常模式识别 + TopologyKB + CaseKB + RAG    │    │
        │  │  输出: attack_type, root_cause, severity,          │    │
        │  │        confidence, evidence                        │    │
        │  └────────────────────────────────┬──────────────────┘    │
        │                                   │                        │
        │  ┌────────────────────────────────▼──────────────────┐    │
        │  │  RunbookAgent → ActionOrchestrator                 │    │
        │  │  → Verifier → Replanner                            │    │
        │  │  (基于 TriageResult 诊断结论，而非 input label)     │    │
        │  └───────────────────────────────────────────────────┘    │
        └──────────────────────────────────────────────────────────┘
                                       │
                              ┌────────┴────────┐
                              ▼                 ▼
                          成功: RESOLVED    失败: Legacy PRP Recovery
                                                  │
                                                  ▼
                                             Safety Control
                                          (Rollback + Escalation)
```

## 2.2 架构对比

| 维度 | 旧架构 (Attack-driven) | 新架构 (Metric-driven) |
|------|----------------------|----------------------|
| **数据入口** | STSRS raw event 含 attack_code | STSRS 原始指标（attack_info 被隐藏） |
| **攻击类型判断时机** | EventNormalizer 阶段（查表映射） | TriageAgent 阶段（LLM+KB 诊断） |
| **Severity 评估** | `AttackType → Severity` 查表 | 指标影响程度动态评估 |
| **Pipeline 路由** | 按 AttackType 分发不同 Pipeline | 统一进入 TriageAgent 诊断 |
| **去重依据** | attack_type + source_ip | train_id + signal_id + 异常指标 |
| **TriageAgent 角色** | 验证已知攻击类型 | ★ 真正的诊断 Agent ★ |
| **RunbookAgent KB 查询** | 基于 Incident.attack_type | 基于 TriageResult 诊断结论 |

---

# 第三部分 项目目录结构

```
railways_V.2/
├── app/                          ← 应用核心
│   ├── data/                     ← [NEW] 数据层
│   │   ├── __init__.py
│   │   ├── stsrs_adapter.py      ← STSRS 数据适配器
│   │   └── prometheus_simulator.py ← Prometheus 模拟器
│   ├── models/                   ← 数据模型
│   │   ├── incident.py           ← 核心事件模型（含 Severity.UNKNOWN）
│   │   ├── metrics.py            ← [NEW] 指标模型
│   │   ├── aiops.py
│   │   └── ...
│   ├── events/                   ← 事件处理层
│   │   ├── event_normalizer.py   ← [MODIFIED] Metric-driven 归一化
│   │   ├── deduplicator.py       ← [MODIFIED] 指标驱动的去重
│   │   ├── severity_engine.py    ← [REWRITTEN] 基于指标影响的评估
│   │   └── timeout_manager.py
│   ├── core/                     ← 核心基础设施
│   │   ├── incident_router.py    ← [SIMPLIFIED] 统一路由（不再按类型分发）
│   │   ├── state_machine.py
│   │   ├── incident_store.py
│   │   ├── audit_store.py
│   │   └── ...
│   ├── agents/                   ← Multi-Agent 诊断链
│   │   ├── triage_agent.py       ← [REWRITTEN] 真正诊断 Agent
│   │   ├── runbook_agent.py      ← [MODIFIED] 基于诊断结果的 KB 查询
│   │   ├── action_orchestrator.py
│   │   ├── verifier.py
│   │   └── replanner.py
│   ├── agent/                    ← Legacy PRP Recovery Engine
│   │   └── aiops/
│   ├── services/
│   │   └── aiops_service.py      ← [MODIFIED] 新增 process_metrics()
│   ├── api/
│   │   └── aiops.py              ← [MODIFIED] 新增 /api/aiops/metrics
│   ├── tools/
│   └── main.py
├── mcp_servers/
├── static/
├── aiops-docs/
├── docs/
└── 铁路智能运维系统源码详解.md    ← [REWRITTEN] 本文档
```

---

# 第四部分 新增模块详解

## 4.1 STSRS Data Adapter（`app/data/stsrs_adapter.py`）

### 为什么需要？

真实 AIOps 场景中，系统只能接收原始监测指标（丢包率、延迟、信号状态等），不能提前知道"这是 DoS 攻击"。STSRS 数据集包含 `AttackInfo` 标签，必须在数据入口处剥离。

### 核心职责

1. **读取 STSRS 数据集** — 支持 CSV、JSON、JSONL 格式
2. **隐藏 AttackInfo** — 自动识别并删除所有攻击标签字段
3. **多文件三维融合** — 按 `Timestamp + TrainID + SignalID` 合并数据
4. **字段名归一化** — 处理不同格式的字段名变体

### 关键设计

```python
# app/data/stsrs_adapter.py

# 需要被删除的攻击相关字段
_ATTACK_FIELDS_TO_HIDE = frozenset({
    "attack_info", "attack_code", "attack_type",
    "attack_label", "label", "is_attack", "ground_truth",
})

class STSRSAdapter:
    def load_and_fuse(self, file_paths: List[str]) -> List[RailMetricRecord]:
        """读取多个文件，按三维键融合"""
        # 1. 逐文件读取
        # 2. 逐行删除攻击字段
        # 3. 字段名归一化
        # 4. 按 FusionKey(Timestamp, TrainID, SignalID) 分组
        # 5. 每组内合并互补指标
        # 6. 输出 RailMetricRecord 列表
```

### 输出：RailMetricRecord

```python
class RailMetricRecord:
    record_id: str          # "RMR-A1B2C3D4"
    timestamp: datetime     # 监测时间
    train_id: str           # "T001"
    signal_id: str          # "S001"
    metrics: RailMetrics    # {speed, packet_loss, latency, ...}
    source_files: List[str] # 来源文件列表
```

### 数据融合示例

```
文件A: Timestamp=12:00, TrainID=T001, SignalID=S001, PacketLoss=0.35
文件B: Timestamp=12:00, TrainID=T001, SignalID=S001, Latency=250
        ↓ 融合 (同一时间/列车/信号设备)
RailMetricRecord{ train_id="T001", signal_id="S001",
                  metrics={packet_loss:0.35, latency:250} }
```

---

## 4.2 Prometheus Simulator（`app/data/prometheus_simulator.py`）

### 为什么需要？

模拟真实生产环境中 Prometheus 从 STSRS Exporter 采集指标的行为。将 `RailMetricRecord` 转换为 Prometheus 风格的时序指标。

### 核心职责

1. **MetricExporter** — 将 `RailMetricRecord` 转为 `PrometheusMetricSnapshot`
2. **标签化** — 状态类指标（SignalStatus, Location）转为 Prometheus labels
3. **Prometheus Text 格式输出** — 兼容 Prometheus exposition format

### 指标名映射

```python
_METRIC_NAME_MAP = {
    "speed":             "rail_speed",
    "distance":          "rail_distance",
    "packet_loss":       "rail_packet_loss",
    "latency":           "rail_latency",
    "renewal_interval":  "rail_renewal_interval",
    "burstiness":        "rail_burstiness",
    "overlap_count":     "rail_overlap_count",
}
```

### 输出格式示例

**Prometheus Text Exposition:**
```
rail_packet_loss{train="T001",signal="S001",signal_status="RED"} 0.35 1721635200
rail_latency{train="T001",signal="S001",signal_status="RED"} 250.0 1721635200
rail_speed{train="T001",signal="S001",signal_status="RED"} 120.0 1721635200
```

**JSON 格式（PrometheusMetricSnapshot）:**
```json
{
  "snapshot_id": "PMS-A1B2C3D4",
  "timestamp": "2024-01-01T12:00:00",
  "labels": {"train": "T001", "signal": "S001", "signal_status": "RED"},
  "metrics": {"rail_packet_loss": 0.35, "rail_latency": 250.0}
}
```

---

# 第五部分 核心模块重构详解

## 5.1 EventNormalizer（`app/events/event_normalizer.py`）

### 旧设计 vs 新设计

| 方面 | 旧设计 | 新设计 |
|------|--------|--------|
| STSRS 处理 | `attack_code → AttackType` 映射 | attack_type = UNKNOWN |
| Prometheus 处理 | `PacketFlood → DoS` 映射 | 保留基础设施告警，攻击类统一 UNKNOWN |
| event_signature | `attack_type + train_id + ip` | `train_id + signal_id + metrics_hash` |
| dedup_key | `attack_type + train_id + ip` | `train_id + signal_id + abnormal_metrics` |

### 关键变化

```python
# 旧版: 直接映射攻击类型（已删除）
STSRS_ATTACK_MAP = {
    "STSRS-1001": AttackType.DOS,       # ← 删除
    "STSRS-1002": AttackType.JAMMING,   # ← 删除
    "STSRS-1003": AttackType.REPLAY_ATTACK,  # ← 删除
}

# 新版: 所有来源 attack_type = UNKNOWN
def _normalize_stsrs(self, raw):
    return Incident(
        attack_type=AttackType.UNKNOWN,  # ← 不在此阶段判断
        severity=Severity.P4,            # ← 由 SeverityEngine 覆盖
        metrics_snapshot=...,            # ← 保留原始指标
    )
```

### 新增方法：normalize_metric()

```python
def normalize_metric(self, metrics_snapshot, source) -> Incident:
    """Metric-driven 核心入口: 将 RailMetricRecord/PrometheusMetricSnapshot 转为 Incident"""
    # 不假设攻击类型
    # 只提取指标快照和基础上下文
    return Incident(
        attack_type=AttackType.UNKNOWN,
        severity=Severity.P4,
        metrics_snapshot=metrics_snapshot,
    )
```

---

## 5.2 Deduplicator（`app/events/deduplicator.py`）

### 旧设计 vs 新设计

| 方面 | 旧设计 | 新设计 |
|------|--------|--------|
| 核心去重键 | `dedup_key` (含 attack_type) | `train_id + signal_id + 时间窗口` |
| 回退键 4 | `atk:DOS:train:T001` | `train:T001:sig:S001:t:202401011200` |
| 回退键 5 | `atk:DoS:sig:S001` | `train:T001:sig:S001` |
| 回退键 6 | `atk:DoS` (最模糊) | `metrics:{hash}` (指标内容 hash) |

### 设计理由

当所有事件 attack_type=UNKNOWN 时，旧的 `atk:Unknown` 去重键会将完全不同的事件错误合并。新的去重基于：
1. **同一设备** (TrainID + SignalID)
2. **同一时间窗口** (分钟级别)
3. **同类异常指标** (相同的异常指标名)

---

## 5.3 SeverityEngine（`app/events/severity_engine.py`）

### 旧设计 vs 新设计

**旧设计（AttackType 驱动）：**
```python
ATTACK_SEVERITY_MAP = {
    AttackType.DOS: Severity.P1,          # DoS = P1 (查表!)
    AttackType.JAMMING: Severity.P1,      # Jamming = P1 (查表!)
    AttackType.REPLAY_ATTACK: Severity.P2, # Replay = P2 (查表!)
}
```

**新设计（Metric Impact 驱动）：**
```python
# P1 阈值 — 影响列车运行安全
SIGNAL_STATUS_DANGER = {"RED", "DANGER", "OFFLINE"}  # 信号异常 → P1
OVERLAP_STATUS_ABNORMAL = {"ABNORMAL", "CONFLICT"}   # 联锁异常 → P1

# P2 阈值 — 影响信号通信
PACKET_LOSS_CRITICAL = 0.5    # 50% 丢包 → P2
LATENCY_CRITICAL = 200.0      # 200ms 延迟 → P2
RENEWAL_INTERVAL_CRITICAL = 5000.0  # 续期间隔 >5s → P2

# P3 阈值 — 性能下降
PACKET_LOSS_MODERATE = 0.1    # 10% 丢包 → P3
LATENCY_MODERATE = 100.0      # 100ms 延迟 → P3
BURSTINESS_MODERATE = 0.5     # 突发度 >0.5 → P3
```

### 评估优先级

```
1. _assess_safety_impact()     → P1 (信号状态/联锁异常/速度异常)
2. _assess_communication_impact() → P2 (高丢包/高延迟/续期间隔异常)
3. _assess_performance_impact()   → P3 (中等丢包/延迟/突发度)
4. 默认                          → P4
```

### 动态升级规则
- 涉及列车的通信中断 → P2 升级到 P1
- 高频重复事件 (>10次) → 升级一级
- 3条以上指标同时异常 → 升级一级

---

## 5.4 IncidentRouter（`app/core/incident_router.py`）

### 旧设计 vs 新设计

**旧设计（按 AttackType 分发 Pipeline）：**
```python
self._pipelines = {
    AttackType.DOS: self._dos_pipeline,
    AttackType.JAMMING: self._jamming_pipeline,
    AttackType.REPLAY_ATTACK: self._replay_pipeline,
}
# route() 中:
pipeline = self._pipelines.get(incident.attack_type, self._default_pipeline)
```

**新设计（统一路由）：**
```python
# route() 中 — 所有事件统一进入 TriageAgent
async for event in self._common_pipeline(incident, thread_id, record):
    yield event
```

所有事件走同一个 `_common_pipeline()`，因为 attack_type 在此时还是 UNKNOWN。真正的差异化处理由 TriageAgent 的诊断结果驱动。

### 更新 Incident 的 attack_type

```python
# TriageAgent 诊断后，更新 Incident 的 attack_type
diagnosed_attack_type = triage_result.attack_type
if diagnosed_attack_type:
    try:
        incident.attack_type = AttackType(diagnosed_attack_type)
    except ValueError:
        # 无法匹配已知枚举，保持 UNKNOWN
        pass
```

---

## 5.5 TriageAgent（`app/agents/triage_agent.py`）— ★ 核心变化 ★

### 角色变化

| 方面 | 旧 TriageAgent | 新 TriageAgent |
|------|---------------|---------------|
| **输入** | Incident (含已知 attack_type) | Incident (attack_type=UNKNOWN, 含 metrics_snapshot) |
| **主要任务** | 验证已知攻击类型、查拓扑 | 分析指标异常模式、诊断攻击类型 |
| **KB 查询** | `"历史案例 DoS 处置"` (基于已知类型) | `"历史案例 高丢包率 高延迟 信号异常"` (基于异常模式) |
| **输出** | TriageResult (root_cause, severity, ...) | TriageResult (**含 attack_type**, root_cause, severity, confidence, evidence) |

### 新增：指标异常分析

```python
def _analyze_metric_anomalies(self, incident) -> Dict:
    """
    分析 metrics_snapshot 中的指标异常模式。
    
    返回:
    {
        "abnormal_metrics": ["packet_loss", "latency"],
        "patterns": ["critical_packet_loss", "critical_latency"],
        "summary": "PacketLoss 50%, Latency 250ms — 典型通信异常",
    }
    """
```

### 新增：基于异常模式的 KB 查询

```python
async def _query_casekb_by_patterns(self, anomaly_analysis, incident):
    """基于异常模式查询历史案例（而非 attack_type）"""
    # 旧版: query = f"历史案例 {incident.attack_type.value}"
    # 新版: query = f"历史案例 高丢包率 高延迟 信号异常"
    pattern_keywords = {
        "critical_packet_loss": "高丢包率",
        "critical_latency": "高延迟",
        "signal_status_abnormal": "信号状态异常",
        ...
    }
```

### 诊断指南（嵌入 Prompt）

LLM 根据指标异常模式判断攻击类型：

| 指标异常模式 | 可能的攻击/故障类型 |
|------------|------------------|
| PacketLoss极高 + Latency极高 + 高Burstiness | DoS 攻击 |
| PacketLoss极高 + SignalStatus异常 + RenewalInterval异常 | Jamming 攻击 |
| RenewalInterval异常 + 重复的Packet序列 | Replay Attack |
| 明显异常的 Speed/Distance 数据 | Spoofing |
| SignalStatus RED/OFFLINE + OverlapStatus ABNORMAL | Signal Interference |
| 只有 PacketLoss/Latency 轻微异常 | 通信故障 (非攻击) |
| 指标正常或仅轻微偏差 | 正常/UNKNOWN |

---

## 5.6 RunbookAgent（`app/agents/runbook_agent.py`）

### 变化

KB 查询不再基于 `incident.attack_type`（因为输入时未知），改为基于 `TriageResult` 的诊断结论：

```python
# 旧版:
query = f"历史案例 {incident.attack_type.value} 处置方案"

# 新版:
diagnosed_type = triage_result.attack_type or ""
root_cause = triage_result.root_cause or ""
query = f"历史案例 {diagnosed_type} {root_cause} 处置方案"
```

---

# 第六部分 完整故障处理案例

## 6.1 场景：STSRS 数据集 → 未知异常诊断（Metric-driven 完整流程）

```
① STSRS Data Adapter
   读取 STSRS 数据集 → 隐藏 AttackInfo
   三维融合 (Timestamp + TrainID + SignalID)
   输出: RailMetricRecord{train_id="T001", signal_id="S001",
          metrics={packet_loss:0.52, latency:320, renewal_interval:8000,
                   signal_status:"RED", burstiness:0.85}}

② Prometheus Simulator
   MetricExporter → PrometheusMetricSnapshot
   rail_packet_loss{train="T001",signal="S001"} 0.52
   rail_latency{train="T001",signal="S001"} 320.0
   rail_renewal_interval{train="T001",signal="S001"} 8000.0

③ EventNormalizer
   normalize_metric() → Incident
   attack_type = UNKNOWN    ← 不假设攻击类型
   severity = P4            ← 由 SeverityEngine 覆盖
   metrics_snapshot = {...}

④ SeverityEngine (Metric Impact)
   _assess_safety_impact():
     signal_status="RED" → P1 风险!
     但不满足联锁异常条件
   _assess_communication_impact():
     packet_loss=0.52 > 0.5 → P2
     latency=320 > 200 → P2
     renewal_interval=8000 > 5000 → P2
   → 因为涉及列车通信 → P2 升级到 P1
   最终: Severity = P1

⑤ TriageAgent ★ 诊断 ★
   指标异常分析:
     - critical_packet_loss (0.52 > 0.5)
     - critical_latency (320ms > 200ms)
     - abnormal_renewal_interval (8000ms > 5000ms)
     - high_burstiness (0.85 > 0.5)
     - signal_status_abnormal (RED)
   
   异常模式: [critical_packet_loss, critical_latency,
              abnormal_renewal_interval, high_burstiness,
              signal_status_abnormal]

   CaseKB 查询: "历史案例 高丢包率 高延迟 信号续期间隔异常 流量突发 信号状态异常"
   TopologyKB 查询: "train T001 signal S001"

   LLM 综合诊断 → TriageResult:
     attack_type: "Jamming"
     root_cause: "Jamming attack causing signal communication failure — 
                  critical packet loss (52%) combined with high latency (320ms) 
                  and abnormal renewal interval (8000ms) indicates 
                  intentional signal interference"
     severity: P1
     confidence: 0.91
     evidence: [
       "Packet loss 52% exceeds critical threshold of 50%",
       "Latency 320ms exceeds critical threshold of 200ms",
       "Renewal interval 8000ms indicates signal disruption",
       "Burstiness 0.85 indicates abnormal traffic pattern",
       "Signal status RED confirms communication breakdown"
     ]

⑥ IncidentRouter 更新 Incident
   incident.attack_type = AttackType.JAMMING  ← 由 TriageAgent 诊断结果设置

⑦ RunbookAgent
   基于 TriageResult 诊断结论查询 KB:
   query = "历史案例 Jamming Jamming attack causing signal communication failure 处置方案"
   生成 RunbookPlan

⑧ ActionOrchestrator → Verifier → Replanner
   (后续流程不变)

⑨ SSE 完整事件流
   incident_created → severity_evaluated → incident_triaged(attack_type=Jamming)
   → plan_generated → action_executed → verification_finished → incident_resolved
```

---

## 6.2 场景：指标轻微异常，无法确定攻击类型

```
指标: packet_loss=0.08, latency=95ms, burstiness=0.3
      (所有指标在正常范围内或轻微偏差)

SeverityEngine: P4 (轻微异常)
TriageAgent 诊断:
  attack_type: None  ← 不强行归类
  root_cause: "Minor network performance degradation — 
              指标异常不显著，无法确定具体故障/攻击类型"
  severity: P4
  confidence: 0.35
  evidence: ["All metrics within normal range or slight deviation"]

→ RunbookAgent 使用保守处置策略:
  - 通知调度员
  - 生成工单
  - 验证网络健康
```

---

# 第七部分 新增 API 入口

## 7.1 `POST /api/aiops/metrics` [NEW]

Metric-driven AIOps 的核心入口。接收原始监测指标数据（不要求提供 attack_type）。

**请求格式：**
```json
{
  "train_id": "T001",
  "signal_id": "S001",
  "timestamp": "2024-01-01T12:00:00",
  "metrics": {
    "speed": 120.0,
    "packet_loss": 0.35,
    "latency": 250.0,
    "renewal_interval": 6000.0,
    "burstiness": 0.8,
    "signal_status": "RED",
    "overlap_status": "NORMAL"
  }
}
```

**也支持 PrometheusMetricSnapshot 格式：**
```json
{
  "labels": {"train": "T001", "signal": "S001"},
  "metrics": {"rail_packet_loss": 0.35, "rail_latency": 250.0}
}
```

**内部流程：**
1. `AIOpsService.process_metrics()` 包装为 raw_event
2. `EventNormalizer.normalize_metric()` → Incident (attack_type=UNKNOWN)
3. 走完整 Event Driven Pipeline（含 TriageAgent 诊断）

---

# 第八部分 数据模型变化

## 8.1 Incident 新增字段

```python
class Incident(BaseModel):
    # 新增
    metrics_snapshot: Optional[Dict[str, Any]] = None
    #   ↑ 原始监测指标快照
    #     格式: {"packet_loss": 0.35, "latency": 250, ...}
    #     由 EventNormalizer 在归一化时填充
```

## 8.2 Severity 新增 UNKNOWN

```python
class Severity(str, Enum):
    P1 = "P1"       # 紧急 - 影响列车运行安全
    P2 = "P2"       # 高 - 影响信号通信
    P3 = "P3"       # 中 - 性能下降
    P4 = "P4"       # 低 - 轻微异常
    UNKNOWN = "UNKNOWN"  # [NEW] 尚未评估
```

## 8.3 TriageResult 新增 attack_type

```python
class TriageResult(BaseModel):
    root_cause: str                         # 根因分析
    attack_type: Optional[str] = None       # [NEW] TriageAgent 诊断出的攻击/故障类型
    severity: Severity                      # 严重级别
    impact_scope: List[str]                 # 影响范围
    confidence: float                       # 置信度
    evidence: List[str]                     # 诊断证据
```

## 8.4 新增指标模型

| 模型 | 用途 |
|------|------|
| `RailMetrics` | 铁路信号系统核心监测指标 |
| `RailMetricRecord` | STSRS Adapter 输出的统一监测记录 |
| `PrometheusMetricSnapshot` | Prometheus Simulator 输出的指标快照 |
| `FusionKey` | `Timestamp + TrainID + SignalID` 数据融合键 |
| `MetricAnomaly` | 单个指标的异常分析结果 |
| `MetricAnomalyReport` | 完整的指标异常分析报告 |

---

# 第九部分 文件变化汇总

## 新建文件 (4个)

| 文件 | 职责 |
|------|------|
| `app/data/__init__.py` | 数据层模块入口 |
| `app/data/stsrs_adapter.py` | STSRS 数据适配器 |
| `app/data/prometheus_simulator.py` | Prometheus 模拟器 |
| `app/models/metrics.py` | 指标相关数据模型 |

## 修改文件 (13个)

| 文件 | 变化程度 | 核心变化 |
|------|---------|---------|
| `app/models/incident.py` | 🟡 中 | Severity.UNKNOWN, Incident.metrics_snapshot, TriageResult.attack_type |
| `app/models/__init__.py` | 🟢 轻 | 导出新增 metrics 模型 |
| `app/events/event_normalizer.py` | 🔴 重写 | 删除 STSRS_ATTACK_MAP, 删除攻击映射, 新增 normalize_metric(), dedup key 重构 |
| `app/events/deduplicator.py` | 🟡 中 | 去重键改为 metric-based, 删除 attack_type 依赖 |
| `app/events/severity_engine.py` | 🔴 重写 | 从 AttackType→Severity 查表 改为 Metric Impact 动态评估 |
| `app/events/__init__.py` | 🟢 轻 | 更新 docstring |
| `app/core/incident_router.py` | 🟡 中 | 删除 per-attack Pipeline 注册表, 统一路由, 新增 TriageAgent 诊断结果处理 |
| `app/core/__init__.py` | 🟢 轻 | 移除 IncidentRouter eager import（修复循环导入） |
| `app/agents/__init__.py` | 🟡 中 | 改为 lazy imports（修复循环导入） |
| `app/agents/incident_router.py` | 🟡 中 | 同步 core/incident_router 的变化 |
| `app/agents/triage_agent.py` | 🔴 重写 | 从"验证已知类型"变为"真正诊断", 新增指标异常分析, 基于异常模式的 KB 查询 |
| `app/agents/runbook_agent.py` | 🟡 中 | KB 查询基于 TriageResult 而非 Incident.attack_type |
| `app/services/aiops_service.py` | 🟢 轻 | 新增 process_metrics() 方法 |
| `app/api/aiops.py` | 🟢 轻 | 新增 POST /api/aiops/metrics 端点 |

## 未修改文件

| 文件 | 原因 |
|------|------|
| `app/agents/action_orchestrator.py` | 不依赖 attack_type |
| `app/agents/verifier.py` | 不依赖 attack_type |
| `app/agents/replanner.py` | 不依赖 attack_type |
| `app/core/state_machine.py` | 不依赖 attack_type |
| `app/core/incident_store.py` | 不直接依赖 |
| `app/core/audit_store.py` | 不直接依赖 |
| `app/tools/*` | 工具层不变 |
| `app/agent/aiops/*` | Legacy PRP Recovery Engine 不变 |
| `app/config.py` | 无需额外配置 |

---

# 第十部分 设计决策 FAQ

## Q: 为什么不删除 AttackType 枚举？
A: AttackType 仍然是有效的类型标签。变化的是**来源**：旧版来自 Normalizer 的映射表（输入），新版来自 TriageAgent 的诊断结论（输出）。删除枚举会导致 Fallback、RunbookAgent 等处的类型检查代码需要大规模重构。

## Q: Severity 为什么保留两阶段评估？
A: SeverityEngine（确定性规则，毫秒级）提供**快速初步评估**用于事件排序。TriageAgent（LLM+KB，秒级）提供**深度确认**可修正初步评估。两者互补。

## Q: IncidentRouter 统一路由后还有存在价值吗？
A: 有。IncidentRouter 仍是编排层——负责按顺序调用 TriageAgent → RunbookAgent → ActionOrchestrator → Verifier → Replanner，并管理状态机迁移和 SSE 事件推送。

## Q: 如果 TriageAgent 诊断错误怎么办？
A: 三层防御：
1. TriageAgent 输出 confidence（置信度），低置信度会触发保守处置策略
2. Verifier 验证执行结果，判断是否真正解决问题
3. 失败自动进入 Legacy PRP Recovery Engine，最多 3 次恢复尝试

---

# 附录：新旧架构对比速查表

| 维度 | 旧 (Attack-driven) | 新 (Metric-driven) |
|------|-------------------|-------------------|
| 输入 | STSRS raw event 含 attack_code | STSRS 原始指标（attack_info 已隐藏） |
| 数据入口 | EventNormalizer 直接映射 | STSRSAdapter → PrometheusSimulator → EventNormalizer |
| attack_type 来源 | Normalizer 阶段 (STSRS_ATTACK_MAP 查表) | TriageAgent 阶段 (诊断结论) |
| Severity 来源 | AttackType 查表 (DoS=P1, Replay=P2) | 指标影响动态评估 (安全/通信/性能) |
| Pipeline 分发 | 按 AttackType 选择不同 Pipeline | 统一进入 TriageAgent |
| 去重键 | `attack_type + source_ip` | `train_id + signal_id + 异常指标` |
| TriageAgent | 验证已知类型 | ★ 真正诊断 ★ |
| KB 查询 | `"历史案例 DoS"` | `"历史案例 高丢包率 高延迟 信号异常"` |
| RunbookAgent fallback | 按 attack_type 硬编码动作 | 按诊断结论 + root_cause 选择动作 |
| API 入口 | `/api/aiops/stsrs` | 新增: `/api/aiops/metrics` |

---

**《铁路智能运维系统源码详解》终**

> 架构版本：Metric-driven Autonomous AIOps
> 核心变化：从 Attack-driven（攻击类型已知 → 查 KB 对号入座）转变为 Metric-driven（原始指标 → TriageAgent 诊断 → 产出攻击类型）
> 所有分析基于真实源码。每一段代码都标注了来源和变化原因。
